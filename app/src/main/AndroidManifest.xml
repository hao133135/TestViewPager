<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.testviewpager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Layout4Activity"></activity>
         <activity android:name=".registeActivity"></activity>
        <activity android:name=".Password.Retrievehepassword"></activity>
        <activity android:name=".Password.password_two"></activity>
        <activity android:name=".lifeActivity"></activity>
        <activity android:name=".Password.Login_reset_newpassword"></activity>
        <activity android:name=".driving_coach.CoachActivity"></activity>
        <activity android:name=".driving_student.StudentActivity"></activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
</manifest>